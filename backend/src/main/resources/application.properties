spring.application.name=backend

spring.profiles.active=test

# Habilita o Spring a gerenciar as transações até a camada de Service
spring.jpa.open-in-view=false

security.client-id=${CLIENT_ID:myclientid}
security.client-secret=${CLIENT_SECRET:myclientsecret}
security.jwt.duration=${JWT_DURATION:86400}
cors.origins=${CORS_ORIGINS:http://localhost:3000,http://localhost:5173}

email.password-recover.token.minutes=30
email.password-recover.uri=http://localhost:8080/recover-password/

#logging.level.org.springframework=WARN
#logging.level.org.springframework.web.servlet.DispatcherServlet=WARN
#logging.level.org.springframework.boot.web.embedded.tomcat=WARN
#logging.level.root=WARN